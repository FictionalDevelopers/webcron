{
  "name": "@webcron/server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc --build",
    "watch": "tsc --watch",
    "dev": "yarn build && concurrently \"yarn watch\" \"nodemon dist/index.js\""
  },
  "dependencies": {
    "@types/cors": "^2.8.8",
    "@types/node": "^14.14.10",
    "@webcron/config": "1.0.0",
    "@webcron/entities": "1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6",
    "typescript": "^4.1.2"
  }
}
